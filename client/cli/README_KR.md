# Alphred CLI 클라이언트

[![English Version](https://img.shields.io/badge/Language-English-red)](README.md)

이 디렉토리는 Alphred Concierge 서버와 상호작용하기 위한 커맨드 라인 인터페이스(CLI) 클라이언트를 포함합니다.

## 🏗 아키텍처
CLI는 사용자의 주요 입출력 장치 역할을 하는 경량 '더미 터미널'입니다.
-   **프로토콜**: HTTP/REST (POST `/chat`)
-   **인증**: Bearer 토큰 / 커스텀 헤더 (`x-alphred-token`)
-   **역할**: 사용자 메시지를 Concierge에게 전송 -> 답변을 출력.

## ⚙️ 설정 (Configuration)

`.env` 파일을 생성하거나 환경 변수를 설정하세요:

```ini
# Concierge 서버 주소
ALPHRED_SERVER_URL=http://localhost:8000

# 인증 토큰 (서버 설정과 일치해야 함)
ALPHRED_ACCESS_TOKEN=your-client-secret-token
```

## 🖥 사용법 (Usage)

### 클라이언트 시작

```bash
# Python 스크립트인 경우 (예시)
python client.py
```

*(참고: 구현 언어(Python/Go/Rust 등)에 따라 실행 명령어는 다를 수 있습니다.)*

### 대화형 모드
시작하면 자연스럽게 대화할 수 있습니다.
-   **User**: "바탕화면 정리 좀 해줘"
-   **Alphred**: "네, 작업자에게 작업(#101)을 지시했습니다. 완료되면 알려드릴게요."

### 명령어
-   `/exit` 또는 `/quit`: 클라이언트 종료.
-   `/clear`: 화면 지우기.
